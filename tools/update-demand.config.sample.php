<?php
/**
 * Настройки для update-demand.php — образец.
 *
 * Как пользоваться:
 *   1. Скопируйте этот файл рядом со скриптом и назовите update-demand.config.php.
 *   2. Впишите свой контакт в hh_ua (это требование hh: они просят указывать,
 *      кто обращается к API).
 *   3. Если хотите обновлять ещё и частотность Вордстата — впишите wf_token.
 *   4. Положите файл ВНЕ папки public_html (чтобы его нельзя было скачать из браузера),
 *      например: /home/uXXXXXXX/nishabuh-config.php, и передайте путь вторым аргументом:
 *        php tools/update-demand.php public_html/nishabuh-data.json /home/uXXXXXXX/nishabuh-config.php
 *
 * ВАЖНО: не храните этот файл в папке сайта и не выкладывайте его в интернет.
 */

return array(
    // Кто обращается к API hh (обязательно укажите реальную почту):
    'hh_ua' => 'NishaBuh/1.0 (contact: ваш-адрес@example.ru)',

    // Токен Яндекс.Директ API для Вордстата. Пусто — частотность не обновляется.
    'wf_token' => '',

    // Регион Вордстата: 225 = Россия.
    'wf_region' => 225,

    // Паузы между запросами (миллисекунды). Меньше — быстрее, но выше риск блокировки.
    'delay_ms'    => 1200,
    'wf_delay_ms' => 800,
    'wf_waits'    => 8,

    // Сохранять копию базы перед записью (nishabuh-data.json.bak).
    'backup' => true
);
